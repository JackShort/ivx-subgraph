{
  "name": "ivx-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ JACKSHORT/IV",
    "create-local": "graph create --node http://localhost:8020/ JACKSHORT/IV",
    "remove-local": "graph remove --node http://localhost:8020/ JACKSHORT/IV",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 JACKSHORT/IV"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "@protofire/subgraph-toolkit": "0.1.2"
  }
}
